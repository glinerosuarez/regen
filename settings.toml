[default]
app_name = "regen"

ticks_per_episode = 60  # at 1 tick per second, this means that an episode last 1 hour - window_size minutes aprox.
[development]
db_type = "sqlite"
db_name = "test_regen"
bnb_base_url = ["https://testnet.binance.vision"] # The base endpoint this client will send requests to.
[production]
db_type = "sqlite"
db_name = "regen"
bnb_base_url = ["https://api.binance.com", "https://api1.binance.com", "https://api2.binance.com", "https://api3.binance.com"]